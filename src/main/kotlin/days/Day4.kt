package days

class Day4 : Day(4) {
    override fun partOne(): Any {
        val numbers = inputList[0].split(",").map { it.toInt() }
        val boardInputList = inputList.drop(2)
        return 0
    }

    override fun partTwo(): Any {
        TODO("Not yet implemented")
    }
}
